# Anthropic API
ANTHROPIC_API_KEY=your_key_here

# Optional: NewsAPI (Stage 1 — if RSS coverage is thin)
NEWS_API_KEY=your_key_here

# Optional: GDELT requires no key (public), but note the endpoint here for reference
# GDELT_API_BASE=https://api.gdeltproject.org/api/v2
